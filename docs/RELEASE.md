# Release process

1. Bump version in `composer.json` and update [CHANGELOG.md](CHANGELOG.md) with the release date and any final notes.
2. Commit, tag (e.g. `v1.0.0`), and push. The release workflow will update the GitHub Release with the changelog.
3. Publish the package to Packagist if applicable.
