services:
  _defaults:
    autowire: true
    autoconfigure: true

  # A2lix TranslationsType has constructor args the bundle does not bind (abstract in container)
  a2lix_translation_form.form.type.translations_type:
    class: A2lix\TranslationFormBundle\Form\Type\TranslationsType
    arguments:
      $globalExcludedChildren: []
      $globalEmbeddedChildren: []
    tags: ['form.type']

  App\:
    resource: '../src/'
    exclude:
      - '../src/Kernel.php'
