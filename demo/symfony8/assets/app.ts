import './app.css';

/**
 * Demo layout (Vite + TypeScript). Nowo Select All Choice and UX Autocomplete
 * are served from their bundles via Twig (`assets:install`); this entry compiles
 * shared styles and any demo-only TS.
 */
if (import.meta.env.DEV) {
  console.log('Form Kit Demo (Vite + TypeScript)');
}
